package model;

import java.util.Calendar;

public class ServiceManager extends Employee{
    public ServiceManager(String id, String fullName, String password, Calendar dateHire) {
        super(id, fullName, password, dateHire);
    }
}
